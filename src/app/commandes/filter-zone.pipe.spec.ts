import { FilterZonePipe } from './filter-zone.pipe';

describe('FilterZonePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterZonePipe();
    expect(pipe).toBeTruthy();
  });
});
