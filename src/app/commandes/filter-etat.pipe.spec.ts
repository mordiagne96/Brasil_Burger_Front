import { FilterEtatPipe } from './filter-etat.pipe';

describe('FilterEtatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEtatPipe();
    expect(pipe).toBeTruthy();
  });
});
