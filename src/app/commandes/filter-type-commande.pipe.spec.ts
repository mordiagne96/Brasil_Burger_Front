import { FilterTypeCommandePipe } from './filter-type-commande.pipe';

describe('FilterTypeCommandePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTypeCommandePipe();
    expect(pipe).toBeTruthy();
  });
});
