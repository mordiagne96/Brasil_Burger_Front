import { FilterPrenomPipe } from './filter-prenom.pipe';

describe('FilterPrenomPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPrenomPipe();
    expect(pipe).toBeTruthy();
  });
});
